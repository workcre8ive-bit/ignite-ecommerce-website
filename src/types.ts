export interface Product {
  id: number;
  name: string;
  price: string;
  image: string;
  tag: string;
}

export interface Category {
  id: number;
  title: string;
  image: string;
  size: string;
}
